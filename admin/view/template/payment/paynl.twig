{{ header }}{{ column_left }}
<div id="content">
  <div class="page-header">
    <div class="container-fluid">
      <div class="float-end">
        <button type="submit" form="form-payment" data-bs-toggle="tooltip" title="{{ button_save }}" class="btn btn-primary"><i class="fa-solid fa-save"></i></button>
        <a href="{{ back }}" data-bs-toggle="tooltip" title="{{ button_back }}" class="btn btn-light"><i class="fa-solid fa-reply"></i></a></div>
      <h1>{{ heading_title }}</h1>
      <ol class="breadcrumb">
        {% for breadcrumb in breadcrumbs %}
          <li class="breadcrumb-item"><a href="{{ breadcrumb.href }}">{{ breadcrumb.text }}</a></li>
        {% endfor %}
      </ol>
    </div>
  </div>
  <div class="container-fluid">
    <div class="alert alert-info"><i class="fa-solid fa-circle-exclamation"></i> {{ text_description }}</div>
    <div class="card">
      <div class="card-header"><i class="fa-solid fa-pencil"></i> {{ text_edit }}</div>
      <div class="card-body">
        <form id="form-payment" action="{{ save }}" method="post" data-oc-toggle="ajax">
          <ul class="nav nav-tabs" id="tabs">
              <li class="nav-item active"><a href="#tab-general" data-bs-toggle="tab" class="nav-link active">{{ tab_general }}</a></li>
              <li class="nav-item"><a href="#tab-settings" data-bs-toggle="tab" class="nav-link">{{ tab_settings }}</a></li>              
          </ul>
          <div class="tab-content">
            <div id="tab-general" class="tab-pane active">              
              
              <div class="row mb-3">
                <label for="input-status" class="col-sm-2 col-form-label">{{ entry_status }}</label>
                <div class="col-sm-10">
                  <div class="form-check form-switch form-switch-lg">
                    <input type="hidden" name="payment_paynl_status" value="0"/>
                    <input type="checkbox" name="payment_paynl_status" value="1" id="input-status" class="form-check-input"{% if payment_paynl_status %} checked{% endif %}/>
                  </div>
                </div>
              </div>   

              <div class="row mb-3">
                <label for="payment_paynl_general_apitoken" class="col-sm-2 col-form-label">{{ text_tokencode }}</label>
                <div class="col-sm-10">                  
                  <input type="text" class="form-control" name="payment_paynl_tokencode" value="{{ tokencode }}"/>             
                </div>
              </div> 

              <div class="row mb-3">
                <label for="payment_paynl_general_apitoken" class="col-sm-2 col-form-label">{{ text_apitoken }}</label>
                <div class="col-sm-10">                  
                  <input type="password" class="form-control" name="payment_paynl_apitoken" value="{{ apitoken }}"/>             
                </div>
              </div>   

              <div class="row mb-3">
                <label for="payment_paynl_general_apitoken" class="col-sm-2 col-form-label">{{ text_serviceid }}</label>
                <div class="col-sm-10">                  
                  <input type="text" class="form-control" name="payment_paynl_serviceid" value="{{ serviceid }}"/>            
                </div>
              </div>    

               <div class="row mb-3">
                <label for="payment_paynl_general_testmode" class="col-sm-2 col-form-label">{{ text_testmode }}</label>
                <div class="col-sm-10">
                  <div class="form-check form-switch form-switch-lg">
                    <input type="hidden" name="payment_paynl_testmode" value="0"/>
                    <input type="checkbox" name="payment_paynl_testmode" value="1" id="input-status" class="form-check-input"{% if testmode %} checked{% endif %}/>
                  </div>
                </div>
              </div>  

              <div class="row mb-3">
                <label for="input-sort-order" class="col-sm-2 col-form-label">{{ entry_sort_order }}</label>
                <div class="col-sm-10">
                  <input type="text" name="payment_paynl_sort_order" value="{{ payment_paynl_sort_order }}" placeholder="{{ entry_sort_order }}" id="input-sort-order" class="form-control"/>
                </div>
              </div>

            </div>    

            <div id="tab-settings" class="tab-pane">      

              <div class="row mb-3">
                <label for="input-logging" class="col-sm-2 col-form-label">{{ text_logging }}</label>
                <div class="col-sm-10">
                  <div class="form-check form-switch form-switch-lg">
                    <input type="hidden" name="payment_paynl_logging" value="0"/>
                    <input type="checkbox" name="payment_paynl_logging" value="1" id="input-status" class="form-check-input"{% if pay_logging %} checked{% endif %}/>
                  </div>
                  <p class="help-block">{{ text_logging_tooltip }}</p>
                </div>
              </div> 

              <div class="row mb-3">
                <label for="input-status" class="col-sm-2 col-form-label">{{ text_logging_download }}</label>
                <div class="col-sm-10">
                  <div class="version">                                
                      <a id="download_logs" class="btn btn-primary" href="{{ pay_logging_download }}" target="_BLANK">Download</a>
                  </div>
              </div> 

            </div>   

          </div>
        </form>
      </div>
    </div>
  </div>
</div>
{{ footer }}
